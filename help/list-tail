R4RS 6.3 (list-tail list k) ==> obj

Returns the sublist of LIST obtained by omitting the first K elements.
LIST-TAIL could be defined by

(define (list-tail x k)
  (if (zero? k)
      x
      (list-tail (cdr x) (- k 1))))
